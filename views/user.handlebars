<section class="container">
    <h3 class="user-name">{{user.name}}</h3>
    <section class="user-cars">
        {{#each user.cars as |cars|}}
        {{> car-details}}
        {{/each}}
    </section>
</section>